apiVersion: v1
kind: Service
metadata:
  name: streetfood-service
  namespace: {{ .Values.namespace }}

spec:
  type: {{ .Values.service.type }}

  selector:
    app: streetfood

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}